import React from 'react';
import Layout from '../view/Layout';
import AuthComponnent from '../components/AuthComponent';

const Authorization = () => {
  return (
    <Layout.Register>
      <AuthComponnent />
    </Layout.Register>
  );
};

export default Authorization;
